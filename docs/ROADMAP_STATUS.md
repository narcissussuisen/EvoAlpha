# EvoAlpha v2.0 · 路线图与状态（离线镜像）

> ⭐ **实时看板（唯一权威，请优先看它）：`http://127.0.0.1:8790/`**
> —— 常驻服务，每次请求在服务端重新探测（账本 / 19 个计划任务 / 归档 sha256 / manifest / 测试基线），**按 F5 就是最新**。
>
> **本文件刻意不重复状态。** 此前它把进度也抄了一份，结果单行改了、它没改，长期挂着"R0.5 未做"、"5/11"
> 这类已过期的行（2026-09-12 用户纠偏）。现改为：**只写「去哪看 / 怎么读 / 维护纪律」**，
> 状态一律以看板与 `docs/dashboard/status.json` 为准。
>
> 权威路线：`docs/EVOALPHA_V2_RESTRUCTURE_PLAN.md` ｜ 看板数据源：`docs/dashboard/status.json`
> 最后更新：2026-09-12

---

## 一、总目标（唯一判据，不要漂移）

> **EvoAlpha = 妖板选手决策过程的可执行复刻体，在一套可审计的账本上自主跑 A 股短线，用它自己的前向净值证明有效。**

判据链条：**行为复刻**（诊断轨，衡量"像不像"）与 **收益验证**（净值轨，唯一晋级闸门）**架构级分离**。
依据：9/11 证伪「命中选手行为 94.4%，但全市场反事实显著反向 p=0.00014」。

---

## 二、路线图（阶段与依赖）

```
R0 奠基与账本真实性  ← 唯一串行前置（不完成则净值不可信）
     │
     ├─▶ 轨道 A：R1 人格层（零生产写入）
     ├─▶ 轨道 B：R2 数据层（只读外部）
     └─▶ 轨道 C：R5.0 分层晋级机制（🔒 必须在 R4 之前建成）
                        │
                        ▼
                  R3 决策环 + LLM 裁量（全 shadow，不产生成交）
                        │
                        ▼
                  R4 盘中实时自主放权（产生真实模拟成交 → 观察时钟启动）
                        │
                        ▼
                  R5.1–R5.3 净值判定 / 版本分段 / 评审回滚
```

| 阶段 | 目标 | 进度快照（2026-09-12） |
|---|---|---|
| **R0** | 账本可信、真实性奠基 | **10/11 完成**；仅 R0.6 受交易日制约（最早 9/18） |
| **R1** | 人格 SOP 结构化（37 条规则→二维表 + 裁量点清单 + `decision_digest` 契约） | 未开始 |
| **R2** | 数据层扩建（全市场 ≥3 年日线、板块题材库、资金流、情绪表硬化） | 未开始（含 `sw_l2_names.json` 128/194 缺口） |
| **R3** | 决策环 + LLM 裁量（六段 artifact、schema+快照+可重放、双轨 shadow） | R3.0 双通道就绪已完成 |
| **R4** | 盘中实时自主（决策服务、下单权交决策环、秒级守护、盘后窗口） | 未开始 |
| **R5** | 净值判定与治理（分层晋级、vs 双基准臂、版本分段、20/60 日评审与自动回滚） | 未开始 |

---

## 三、怎么读看板

| 章节 | 用途 | 维护纪律 |
|---|---|---|
| **实时事实** | 脚本探测的客观值（账本 / 任务 / 归档 / manifest / 测试 / WorkBuddy 通道） | 由 `tools/build_roadmap_dashboard.py` 探测，**禁止手写** |
| **需要你出手** | **只放真正卡住施工、非你不可**的事 | ⚠️ **已解决的一律移除**，不留"已解决"占位；为空时显式渲染"✅ 当前无阻塞项" |
| **可选事项** | 不做也能继续的（长跑批授权、推送偏好等） | 与阻塞项**分开**，用 `P3` 配色 |
| **阶段进度** | 各阶段子项状态 + 证据 | 改状态时**同步证据**，别只改状态 |
| **卡点与堵点** | 时间卡点 / 技术堵点 / 数据缺口 | 解决后**从本区删除** |
| **待授权 / 已闭环** | 记决策；已闭环项仅存沿革并标"勿再引用" | 状态随事实推进（`proceeding`→`applied`）并写清**实际发生了什么** |

**改状态的正确姿势**：只改 `docs/dashboard/status.json` → 刷新 8790 即生效。**不要手改 HTML。**

> ⚠️ 服务在内存里缓存生成器模块：**改了 `build_roadmap_dashboard.py` 必须重启服务进程**才生效
> （杀 pythonw → 重新双击 `yaoban-system\scripts\open_dashboard.vbs`）。
>
> 2026-09-14（用户裁定·定时任务全景审计）：原每 5 分钟保活任务 `EvoAlphaRoadmapServer` **已退役**
> （286 次/日 → 0）。看板改为**按需启动**：双击 `yaoban-system\scripts\open_dashboard.vbs`，
> 它会幂等地拉起服务（已在监听则跳过）并打开浏览器；服务一旦起来就常驻，后续点击只开浏览器。

---

## 四、需要理解背景的卡点

实时清单见看板。此处只留一条：

**R0.6「收盘链连续 5 个交易日 rc=0」是 R0 唯一未结项项，且结构上无法提前** —— 需 9/14–9/18 实盘观察。
其余 R0 项已于 2026-09-12 全部完工。

---

## 五、维护纪律（2026-09-12 用户纠偏后确立）

1. **状态只写一处**：`docs/dashboard/status.json`。本文件不再抄状态。
2. **"需要你出手"只放真阻塞项**；已解决项**移除**、可选事项另区。
   看板说需要用户、实际不需要，**就是噪声**。
3. **单行状态与叙事块必须一起改**：`stage items` 改了，`authorizations` / `blockers` / `next_steps`
   里对应的旧描述也要清 —— 此前只改单行，导致板上长期挂着已完工项与已失效的卡点。
4. **证据要能被验**：写进 `evidence` 的断言必须实测过，引用的文件路径必须真实存在。
5. **不轻信看板**：接手时用实物核对（文件是否存在 / 任务是否真注册 / 账本是否真变更）。
