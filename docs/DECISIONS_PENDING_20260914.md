# 待裁定事项说明 · 2026-09-14 夜（附方法论缺口）

> **结论先行**：4 项里**只有 2 项需要你裁定**（①、④）。
> ② 是 ① 的技术后果，裁定 ① 即一并解决；③ 查证后**已不存在，无需裁定**。
> 另附你问的「竞价 + 情绪」方法论问题：**要改，但不必动池的生成时点**。

---

# 第一部分 · 四项裁定说明

## ① `tick_monitor.py` 怎么提交（连带 320 行重构 / 原作者确认 / 暂不交）

### 是什么
工作区的 `scripts/tick_monitor.py` 与 **HEAD 版本差了整整一半以上**：

| 指标 | 值 |
|---|---|
| `git diff --numstat HEAD -- scripts/tick_monitor.py` | **+380 / −59**，**6 hunks** |
| HEAD 版本字符数 | **10,787** |
| 工作区版本字符数 | **26,311**（HEAD 的 **2.4 倍**） |

### 证据（本次实测，非转述）
| 检查 | HEAD | 工作区 | 含义 |
|---|---|---|---|
| `def write_idle_pos_live` | 1（L64） | 1 | 函数体**已在 HEAD** |
| `write_idle_pos_live(` | **1** | **2** | HEAD 只有定义行 ⇒ **inert（定义了不调用）** |
| 调用点定位 | — | **L397** `if hm2>SESSION_AM[1]:write_idle_pos_live(day,n)` | 修复**只在工作区是活的** |
| `AFTER_HOURS_END` | 0 | 5 | 这批「非本次」改动确实都在工作区 |
| `def write_beat` / `brain_sell` | 0 / 0 | 1 / 2 | 同上（心跳解耦 / brain_sell 接线） |

### ⭐ 性质判断（关键，决定该选哪个）
那 380 行**不是「别人未完成的草稿」——它已经在生产里跑了一整天**：
daemon 已按工作区版本重启（`tick_guard_state = closed_ok / daemon_alive=true / restarts_window=0`）、
9/14 半日机械面全绿、13:00 首轮不再 `rc=6`（午休成因已修）。
⇒ **真正的问题是「HEAD 落后于生产」，不是「工作区有垃圾」。**

### 三个选项
| 选项 | 后果 |
|---|---|
| **A. 单独提交整个文件**（附逐 hunk 内容清单 + 全量测试） | HEAD 与生产一致；R3 的「可复现/可重放」验收才在 HEAD 上成立 |
| B. 等「原作者」确认 | 本仓没有独立原作者（改动由 agent 跨会话续写）⇒ 实际等于**无限期挂账** |
| C. 暂不提交 | HEAD 继续不可复现；任何人从 HEAD 部署会**退回「13:00 rc=6 禁新仓」** |

### 我的建议
**选 A，但排到 9/15 收盘后执行**。
- 理由 1：改动**已在生产验证**（有实盘证据），提交语义是「**补登记**」，不是引入新行为。
- 理由 2：按你的准绳 —— 提交**不改变运行行为** ⇒ `replay_hash` 不变 = **实现/登记层** ⇒ **不必进 60 日有效性队列**。
- 理由 3：**9/15 是取样日**，当天不让 HEAD 与生产发生任何变动，免得扰动样本。
- 提交前动作：`git diff` 逐 hunk 过一遍，剔掉明显的调试残留（如 `*.bak_20260` 这类临时物），其余照实登记。

**⇒ 要你给的裁定（一句话）**：`①A，9/15 收盘后执行` ／ `①B` ／ `①C`

---

## ② 午休 `pos_live` 修复未进 HEAD

### 不是独立项 —— 它是 ① 的技术后果
修复的**函数体在 HEAD**（L64，含完整根因注释），**调用点只在工作区**（L397）
⇒ **HEAD 里这个修复是死的**。**裁定 ① 即一并解决 ②。**

### 唯一需要你额外表态的：它的语义放宽
修法让 daemon 在「上午收盘之后」的非执行窗口也写 `pos_live`。代价：

| | 现状 | 修后 |
|---|---|---|
| `pos_live` 语义 | 「交易时段实时快照」 | 「最近一次快照」 |
| `companion_health()` 的 **120s 新鲜度**判据 | 能同时代理「数据新鲜」与「daemon 活着」 | **不再能区分「daemon 死了」与「非交易窗口」** |

（注：今日 09:46 那次**真实停摆**，是靠别的信号发现的，不是靠 pos_live。）

| 选项 | 说明 |
|---|---|
| **a. 接受现修法** | 最小改动、9 项测试 + 已上线一日无异常 |
| b. 改成**双判据** | 窗口内查 `pos_live` 新鲜度、窗口外查 `_tick_daemon.beat` 心跳 —— 语义更干净，但要动 `companion_health`，属**行为改动 ⇒ 进 60 日队列** |

**我的建议：a**（9/15 是取样日，不动判据逻辑），但把「语义放宽」写进 blockers 留痕，别让它无声溜过去。

**⇒ 要你给的裁定**：`②a` ／ `②b`

---

## ③ `yaoban_tick_manual` 是否注销

### 查证结论：**已经不存在了，无需裁定**

| 检查 | 结果 |
|---|---|
| `schtasks /query /tn yaoban_tick_manual /fo list /v` | **rc=1「系统找不到指定的文件」** |
| 全量 `schtasks /query /fo csv /v`（16.3 万字符）搜 `tick_manual` | **0 命中** |

**时间线**：D11 日报（15:52）说它「仍注册在案（23:59 有效）」；**20:2x 那轮「删 11 条僵尸任务」已把它一并删掉**
⇒ 该条建议**已被后续审计执行落地**，D11 日报那句属**过期信息**。

---

## ④ 候选池口径降级落地

### 你的裁定
> 「候选池可能有错答案，实盘买卖贴合才是正确答案，无需过分纠结候选池完美复刻。」

### 这条裁定有实据支撑（我找到了）
`选手学习资料/选手战法画像-累计.md:809`：

> ⚠️ **9/11 出现"双向偏离"**：候选池 3 只只买 1 只（**漏买 2 只**），同时又买入 1 只**池外标的**（**额外买 1 只**）
> → 说明其真实交易集与公开候选池是**两个有交集但不等同的集合**

⇒ **选手本人就会「漏买池内 + 买池外」**。你的裁定不是妥协，是**有实据的口径修正**。

### 要改的 3 处（已拟好，你点头就落）
| # | 位置 | 改法 |
|---|---|---|
| 1 | `docs/PATTERN_QUEUE_SAMPLE_PLAN_20260915.md:271` | `|候选池 ∩ 选手池|` 的标注「**主判据**」→「**诊断参考（不构成验收）**」；§5.3 增补一行「**真值基准 = 选手实盘买卖明细**」 |
| 2 | 同文 §3.1 / §3.2.1 | 「选手 4 只」统一称为「**选手候选池引用集**」；删掉「实买样本」这一混用称谓 —— 它们的 `sig_date` 是 **T-2/T-3**、来自**公开候选池**，**不是成交** |
| 3 | `docs/dashboard/status.json` | blockers[1] 标题「候选池交集 **0/4 → 2/4**」补标「（诊断项）」；`meta` 增一条治理记录：**复刻评价基准 = 实盘买卖行为贴合度；候选池交集率降级为诊断参考** |

### 保留不动
§6 的「选手 **≥3/4** 否则回滚」—— 它是**过滤器防误伤的必要条件闸门**（真买过的票不许被筛掉），不是保真度评分。
**降 KPI ≠ 砍护栏。**

**⇒ 要你给的裁定**：`④改，按上面 3 处`（或指定只改其中几条）

---

## 附：新基准当前「无尺可量」→ 建议立一项工程

落盘里**只有「选手候选池引用集」，没有「选手实盘成交明细」**。新基准要落地，先得有这把尺子。
建议把已有人工记录的成交（如 `选手学习资料/文字资料/2026-09-14_1047_持仓截图/record.md`）
**正规化为基准数据文件**：`learning/labels/player_fills/<date>.json`，
字段 `date / sym / side / px / qty / evidence_path / recorded_at`。**这是 ④ 的前置工程。**

---

# 第二部分 · 方法论问题：竞价 + 情绪要不要接进决策

> **你的问题**：EvoAlpha 的候选盘今天收盘就选出来了，而妖板选手是看**当天集合竞价**与**市场情绪**再决定 —— 方法论是否需要修改？

## 2.1 先分层：你说的「候选盘」只对一半

**T-1 收盘真正冻结的只有三样**：
1. 战法池成员（`2026-09-15_pattern_pool.json`，1546 只，只看到 T-1 及以前的日线）
2. 战法标签与信号日（`pattern / patterns / sig_date / bars_since_sig`）
3. 日计划 4 只 picks（`2026-09-15_plan.json`）

**T 日实时参与的已经有三样**（所以「谁买」不是 T-1 定的）：
| 实时输入 | 位置 |
|---|---|
| 全市场实时行情 → 活跃池（`rough_screen`） | `scan_and_confirm.py:306-330` |
| **板块热度 = T 日实时 L2 涨幅排名前 12**（+ T-1 强主线豁免） | `:68 HOT_L2_TOP=12`、`:355`、`:359` |
| e4 低吸触发价 = **T 日 1m 分时**（回踩 MA5/MA10 + 涨幅∈[2%,3%] + 站上 VWAP） | `:515-547` |

⇒ 选股实际是 `confirm_queue = 战法池 ∩ 今日活跃`，**T 日盘中收窄**。

## 2.2 但你的直觉指中了一个真实缺口 —— 而且是系统自己写明的已知偏离

### 缺口 A：集合竞价数据**采到了、没人用**
- `scripts/auction_monitor.py:1-5` docstring：*"Call-auction observer (09:15-09:29), **strictly read-only** ... It **never writes the ledger**."*
- 冻结产物 `outputs/auction/auction_freeze_<day>.json` 自带 `read_only: true` / **`orders_allowed: false`**（`:36-40`）
- **全仓 grep 消费方**：只有 `collect_daily_acceptance.py`（验收）与 `notify_trading_events.py`（推送「仅观察，不在集合竞价阶段生成模拟订单」）
  ⇒ **决策链（scan / ledger / decision_chain）零消费**。今日 `outputs/auction/` 有 **112 个文件**。

### 缺口 B：情绪用的是 **T-1 收盘**，不是当天
- `scan_and_confirm.py:293-302`：读 `outputs/sentiment_full_2026.csv` **末行** → `emotion_thermometer`
- 该 CSV 由**盘后** `r5p_sentiment_build.py` 生成（今日 mtime **16:18:55**，末行 = `2026-09-14`）
  ⇒ **T 日 09:30 开盘时，温度读的是 T-1 收盘值**

### ⭐ 而且这条修正**你自己早就裁定过**
`scan_and_confirm.py:496-498` 与 `persona/sop_v0.toml:291` 都写着：

> 用户裁定的正确顺序是「**① 早盘外盘 → ② 早盘集合竞价 → ③ 前日收盘**」，**当前只实现了 ③ ⇒ 完整口径待补**

⇒ **所以：方法论要改，但不是改池的生成时点，而是把「按当天情况」这半句补齐**（外盘 + 竞价两档）。

## 2.3 建议的两步走（符合项目纪律：不得引入未标定参数）

**步 1（9/15，只留痕、不改变行为）**
1. 每轮 scan 的 confirm 快照里**并写竞价字段**：该标的在 09:25 冻结时的 `chg`、竞价额排名、是否在 `focus`（计划+持仓）
2. 加算一个「**T 日盘前情绪**」与 T-1 温度**并排留痕**（不替换）

**步 2（样本 ≥5 个交易日后）**
做判别检验：竞价**放量高开** vs **缩量低开**，对 e4 低吸的成功率 / 盈亏**是否有区分度**。
**有区分度才做准入**（否决/加权），否则只当排序权重。

**两条硬约束（照今天的教训）**
1. 竞价数据 **09:25 冻结**，而买点在 09:30 之后 ⇒ 它只能做**预筛 / 排序**，**不能替代分时三引擎**。
2. 任何新闸门在**竞价数据缺失时必须放行而不是禁仓** —— 否则重演 `rc=8` 全天零样本，直接毁掉取样日。

## 2.4 一个反面提醒
「候选盘 T-1 就定死」这个担心**部分不成立**（见 2.1：板块热度与分时确认都是 T 日实时的）。
真正的问题是：**T 日盘前那 15 分钟（09:15–09:30）的信息，系统现在是聋的。** 改这一处即可，不必推翻链路。
