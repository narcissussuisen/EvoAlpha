# 选股层上生产 · 交付与验收（2026-09-14 夜 → 09-15 09:15 前）

> **用户裁定**：旧的选股层完全不符合模式，**今晚必须上生产**；核对双尺子 = **选手当日候选池召回（必要条件）** + **与持仓截图反推的买卖贴合**；分两层落地（先 A 后 B）。
> **本报告 = 交付证据**。所有数字都可一条命令重跑，命令写在每张表下方。

---

## 0.5 ⭐ Layer C（2026-09-15 05:37–06:05 增补，用户裁定「今天实盘前上线」）

**触发**：用户提供雪球《N字型走势一点心得》+ 武汉凡谷实证 ⇒ 改判昨晚结论：武汉凡谷漏检**不是「口径外」，是时序结构差** —— 它 9/8 涨停（放量 2.8 倍），选手 **9/9 09:46** 列入候选池 = 「**T−1 涨停 → 次日晨间观察**」机制；而 `zt_huicai` 要求涨停在信号日之前 ⇒ **池比选手晚一天**。32 只逐只核：**14/32 = 44% 与近 3 日涨停强相关**。

| 项 | 结果 |
|---|---|
| **zt_watch 进池** | 提交 **`006f61b`**：`pattern_pool.py` 新增「涨停次日观察」条目（asof 当日涨停→进池、`bars_since_sig=0`、板块分档判涨停、已命中者不重复加）|
| **lookback 4→6** | 提交 **`ae73dff`**：补回南华期货（信号在 5–6 日前）；`plan_daily.py:140` 用「只打自己补丁」法提交（他人 M 文件未带入）|
| **召回** | **30/32 → 32/32 = 100%**（武汉凡谷由 zt_watch 补回、南华期货由 lookback 补回；8 天逐日无下降）|
| **池** | 954 → **1246**（+31%；lookback 放宽所致；`zt_watch` 纯新增 **8** 只 —— 9/14 涨停 55 家大部分已被其他战法命中，不重复加）|
| **断言** | C 层 8 条全过：`load_pattern_pool` 非 None、`lookback=6`、**C ⊇ B**（B 的 954 只全保留）、zt_watch 条目口径抽查、9/14 名单 **4/4** 在池 |
| **全量回归** | **589 passed / 0 failed**（含 6 项 zt_watch 新用例）|
| **回退链** | `.bak_preC`（B 层 954）→ `.bak_preB`（A 层 1526）→ `.bak_preA`（旧口径 1546）；代码 `git revert 006f61b ae73dff` |
| **过程中的真 bug（测试抓到）** | ① zt_watch 首版 `sig_date` 用了 asof 字符串 ⇒ `dates.index()` 抛 ValueError（asof 可能晚于数据末根）；② 缩进错误把 zt_watch 块嵌进 `for pname` 循环 ⇒ 空列表。两者均由新增单测钉住后修正 |

**三次提交总链**：`ef2cb05`(A 修正) → `0abd37e`(B 收窄) → `ae73dff`(lb6) + `006f61b`(zt_watch)；最终池 `built_at=2026-09-15 06:03:14`、1246 只、`huigui 1133 / zt_huicai 97 / xianren 8 / zt_watch 8`。

---

## 0. 结论先行

| 项 | 结果 |
|---|---|
| **A 层（纯修正）** | 已提交推送 **`ef2cb05`**；池 **1546 → 1526**；`huigui/xianren` **一字不变**；`zt_huicai 99 → 79`（只减不增）；全量回归 **583 passed / 0 failed** |
| **B 层（受控收窄）** | 已提交 **`0abd37e`**（仅 1 文件 +18/−2）；toml 两键（`pullback_days_max 7→5`、`prior_gain_min_pct 8→12`）；池 **1526 → 954（−37.5%）**；**召回不降（30/32 保持）** |
| **召回尺子** | 8 天 / 32 只选手候选池：**基线 30/32 = 93.8%**，B 后仍 **30/32** ⇒ **收窄没付召回代价** |
| **买卖贴合（9/14）** | 个股重合 **0/1**、进场溢价偏差 **+6.70pp**、≤3% 违规 系统 1 / 选手 0 —— ⚠️ 属**进场闸门**遗留，非本轮选股层范畴 |
| **待裁定 1 项** | `lookback 4→6`：可把召回提到 **31/32**，但会**扩池**，且需改 `plan_daily.py`（该文件正被他人改动） |

---

## 1. 病根：两个独立的病（今晚各修一个）

| 病 | 证据 | 今晚负责层 |
|---|---|---|
| **漏**（recall） | 选手 9/14 名单 4 只里 **3 只属「涨停启动→回踩→站上均价线」**（`选手学习资料/文字资料/2026-09-14_0946_候选池/record.md:100-108` 明写「被买入的都是唯一满足该组合的标的」），属 `zt_huicai` 管辖 | **A**（`limit_up_mask` 板块分档） |
| **宽**（precision） | 池 1546 只 ≈ 全市场 **27.4%**；选手原文「用战法筛到**几十只**」（`画像:1172,1178`：04-24=25 只 / 05-12=8 只） | **B**（toml 收窄） |

⭐ **一处关键历史证据**：9/14 的生产池 `built_at = 2026-09-14 13:49:12`，而 `zt_huicai` 恒 False 的修复是 **15:52** 才落地 ⇒ 那份池 `by_pattern` **根本没有 `zt_huicai`**（1713+588+7=2308）⇒ **当天必然漏掉 600644 乐山电力**。**不是数据问题，是产物早于修复。**

---

## 2. 表 A · 8 天选手候选池召回矩阵（尺子 = 必要条件）

尺子文件：`learning/labels/player_pool/<date>.json`（8 天 / **32 只**，人工转录、**全部带 6 位代码**、每项带 `evidence` 文件:行号）。

| 发布日 | asof | 只数 | 基线召回 | 漏检标的 | B 后召回 |
|---|---|---|---|---|---|
| 2026-09-02 | 09-01 | 4 | 4/4 | — | 4/4 |
| 2026-09-03 | 09-02 | 4 | 4/4 | — | 4/4 |
| 2026-09-04 | 09-03 | 5 | 5/5 | — | 5/5 |
| 2026-09-07 | 09-04 | 4 | 4/4 | — | 4/4 |
| 2026-09-09 | 09-08 | 4 | **3/4** | `002194` 武汉凡谷 | **3/4** |
| 2026-09-10 | 09-09 | 4 | **3/4** | `603093` 南华期货 | **3/4** |
| 2026-09-11 | 09-10 | 3 | 3/3 | — | 3/3 |
| 2026-09-14 | 09-11 | 4 | 4/4 | — | 4/4 |
| **合计** | | **32** | **30/32 = 93.8%** | | **30/32 = 93.8%** |

**两条漏检的定性（逐战法单试，`_scratch/explain_miss.out.txt`）**：
- `603093` 南华期货：`zt_huicai` 命中 9/1、9/2，距 asof=9/9 有 **5–6 个交易日** ⇒ **超出 `lookback=4`** ⇒ 属**窗口口径**问题（可修，见 §6 待裁定）。
- `002194` 武汉凡谷：`huigui` 最近命中 8/14–8/18、`zt_huicai` 8/10–8/12 ⇒ **8 个交易日内任何战法都无信号** ⇒ **口径外**（选手该笔很可能来自他自述的另一条入口：「龙头示范下的补涨捕捉」/「概念驱动」，`画像:1153-1154`）⇒ **不应计为 FAIL，也无法由形态战法达到**。

> 复跑：`python learning/tools/player_pool_recall.py --out outputs/player_pool_recall/recall_prod_toml.json`
> 历史交叉：9/14 的**生产产物**（13:49 版）只捕获 **3/4** —— 见 §1 的历史证据。

---

## 3. 表 B · 9/14 买卖贴合（终局尺子）

| 指标 | 公式 | 选手 | 系统（EvoAlpha） | 判定 |
|---|---|---|---|---|
| 买入标的 | — | 博敏电子 `603936`（PCB） | 豫能控股 `001896`（电力） | ❌ 个股重合 **0/1** |
| 是否在候选池内 | — | ✅（09:46 池） | ❌ 池外（`plan_match.in_plan=false`） | ❌ |
| 买入价 / 溢价 | `px/昨收−1` | 5,000 股 @21.42664 → **+2.61%** | 11,600 股 @12.91 → **+9.31%** | ❌ 偏差 **+6.70pp** |
| ≤3% 硬约束违规 | `#{溢价>3%}` | 0 | 1 | ❌ |
| 单笔占权益 | — | 29.33% | 29.98% | ✅ Δ+0.65pp |
| 当日卖出笔数 | — | 0 | 0 | ✅ |

⚠️ **必须分清归属**：那笔 +9.31% 的越界是 **`e4_support` 进场上界缺失**造成的，而该缺口**今日 12:0x 已修**（`scan_and_confirm.py:46,543` `E4_MAX_PCT=0.03`）。⇒ **它不属于本轮选股层范围**，本轮只负责「该在池里的在不在池里」。
👉 本表的真正价值：**池内率**（系统买入是否落在自己的池内）—— 9/14 系统买的是池外标的，属执行被 off-plan 抢占，已在 `status.json` 登记。

> 选手侧出处：`选手学习资料/文字资料/2026-09-14_1047_持仓截图/record.md`；系统侧：`portfolio/ledger.json::account.fills`。

---

## 4. Layer A · 改了什么（纯修正，不改宽窄）

4 文件 / 5 处（**缺一处则 `symbol` 传不进去 = 改了等于没改**）：

| # | 文件:行 | 改动 |
|---|---|---|
| A1 | `src/core/indicators.py:54` | `limit_up_mask(df, pct_threshold=9.8, symbol=None)`：给 symbol 按 `core.sell.limit_pct_of` 判（0.2pp 容差）⇒ 主板 0.098 **与旧 9.8% 完全等价**，20cm→0.198 / 30cm→0.298 ⇒ 只减不增；不给 symbol 走旧分支 |
| A2 | `src/core/strategies.py:120` | `detect_zt_huicai(df, symbol=None)` + 修 docstring「近 2~7 天」（配置实为 1~5） |
| A3 | `src/core/strategies.py:128` | 调用点传 `symbol` |
| A4 | `src/core/pattern_pool.py:41-46` | `DETECTORS` 四个检测器统一**双参** `(df, sym)` |
| A5 | `src/core/pattern_pool.py:168` | `mask = fn(d, sym)` |
| — | `tests/test_pattern_pool_filters.py` | 两处**替身**同步双参（契约变更） |

**影响面（已核实）**：`git grep limit_up_mask` ⇒ 生产调用方**唯一** `strategies.py:128`；`git grep detect_zt_huicai` ⇒ 生产链仅 `pattern_pool.py:43`。⇒ 只影响 `zt_huicai` 一个战法。

**验收（重建 9/15 池，asof=09-14）**：池 **1546 → 1526**；`huigui 1444`、`xianren 3` **一字不变**；`zt_huicai 99 → 79`；集合不变量 **新池 ⊆ 旧池**；`load_pattern_pool('2026-09-15')` 非 None；全量 **583 passed / 0 failed**。

### ⚠️ 过程中发现的一个静默降级（值得记录）
改 `DETECTORS` 签名后，`tests/test_pattern_pool_filters.py` 的 4 个用例立刻失败 —— 因为它们把 `DETECTORS` 换成**单参替身**，双参调用抛 `TypeError`，被 `build_pattern_pool` 里那个宽 `except Exception: continue` **静默吞掉**（池直接变空、不报错）。
**教训**：该函数的宽 except 会吞掉任何签名/契约错误 ⇒ **改检测器契约时，必须同时改测试替身**，并靠「池规模断言」兜底。

---

## 5. Layer B · 收了什么（toml 两键，零代码风险）

`config/parameters.toml [strategy.huigui.live]`（**live 模式唯一生效处**）：

| 键 | 原值 | 新值 | 依据 |
|---|---|---|---|
| `pullback_days_max` | 7 | **5** | 8 天召回不降；代理保留率 81.6%（收窄 ~18%）；且与顶层同键对齐 |
| `prior_gain_min_pct` | 8.0 | **12.0** | 8 天召回不降；代理保留率 72.4%（收窄 ~28%） |

**B 层实测结果**（重建 9/15 池，`built_at=2026-09-15 01:17:45`，7 条断言全过）：

| 指标 | A 层 | B 层 | 变化 |
|---|---|---|---|
| 总池 | 1526 | **954** | **−572（−37.5%）** |
| `huigui` | 1444 | **856** | −588（−40.7%） |
| `zt_huicai`（首命中标签口径） | 79 | 95 | ⚠️ **标签迁移**（见下），非新增 |
| `xianren` | 3 | 3 | 不变 |
| 池占全市场规模 | 27.0% | **16.9%** | — |
| 8 天召回 | 30/32 | **30/32** | **不降** ✅ |

⚠️ **一个必须记住的口径陷阱（今晚踩到）**：`by_pattern` 统计的是「**首个**命中战法」
（`pattern_pool.py` `pattern = hit_patterns[0]`，而 `patterns` 元组顺序里 `huigui` 在前）
⇒ 收紧 `huigui` 后，原本「huigui+zt_huicai 共振」的票被**改记**到 `zt_huicai` 组
⇒ `by_pattern` 出现「zt_huicai 增加」的**假象**（实测仅 **16 / 954** 只票改标签）。
**真正的不变量是「逐票 `patterns` 集合只减不增」**（已作为断言 5 固化），
`by_pattern` 只能当留痕、**不可当单调性判据**。

**受控试验全表**（单变量；召回 = 8 天 32 只，保留率 = 250 只旧池样本）：

| 旋钮 | 8 天召回 | 保留率 | 判定 |
|---|---|---|---|
| baseline（生产默认） | 30/32 | 99.6% | — |
| `pullback_days_max=5` | 30/32 | 81.6% | ✅ **采纳** |
| `pullback_days_max=6` | 30/32 | 96.0% | 可用但收窄弱 |
| `prior_gain_min_pct=12` | 30/32 | 72.4% | ✅ **采纳** |
| `prior_gain_min_pct=10` | 30/32 | 82.4% | 可用（较弱） |
| `volume_ratio_max=0.8/0.85` | 30/32 | 94.4/97.6% | ⚠️ 幅度太小 |
| `pullback_pct_max=12/10` | 30/32 / **28/32** | 98.0/93.2% | ❌ 10 伤召回 |
| **`pullback_days_min=2`** | **26/32** | 86.4% | ❌ **伤 4 只**（含选手实买的 600644 / 603328） |
| **两键叠加（上线值）** | **30/32** | — | ✅ |

⚠️ `pullback_days_min=2` 丢掉的正是**选手当天实际买入的那两只** ⇒ 完美重演 `pattern_pool.py:120-124` 记的那条禁区（**凭直觉收窄会筛掉真值**）。
标定依据与**被拒绝的收窄**已一并写进 toml 注释（防日后有人"优化"回来）。

---

## 6. 待你裁定（1 项）

**`lookback 4 → 6`**（`build_pattern_pool`/`plan_daily` 的**信号日回溯窗口**，非 toml 键）：

| | 收益 | 代价 |
|---|---|---|
| 召回 | **30/32 → 31/32**（补回 9/10 南华期货） | — |
| 池规模 | — | 非池样本新增 **21/150 = 14%** ⇒ **扩池**（方向与"选股层太宽"相反） |
| 工程 | — | 需改 `plan_daily.py:140`（写死 `lookback=4`），而**该文件当前是 `M`（他人未提交改动）** ⇒ 会卷入他人改动 |

⇒ 今晚**没做**，理由是：① 与"收窄"方向相反，值得你单独决策；② 需要动并发文件。
**若你点头**，做法是：`plan_daily.py:140` + `build_pattern_pool.py:38` 的默认值同步改 6，跑一次全市场重建（~35min），用同一张 8 天矩阵验收（预期 31/32）后提交。

---

## 7. 回退链（产物不在 git，`.gitignore:21`）

| 产物 | 内容 | 回退动作 |
|---|---|---|
| `outputs/patterns/2026-09-15_pattern_pool.json` | 当前 = **B 层口径** | — |
| `...json.bak_preB` | **A 层成果**（1526 / zt_huicai 79） | `copy .bak_preB → 正式名` |
| `...json.bak_preA` | **原始生产口径**（1546 / zt_huicai 99） | `copy .bak_preA → 正式名` |
| 代码 | A 层 | `git revert ef2cb05`（仅 4 文件） |
| toml | B 层两键 | 单行改回 `7` / `8.0`（**勿整文件回退**，会丢他人 `[fees]` 等改动） |
| 明早 fail-closed rc=8 | — | 立即重跑重建（~35min，开盘前可救）；来不及则临时去掉 `run_trading_task.ps1:69` 的 `--pattern-gate`（`:68` 注释已写明这是回退开关） |

---

## 8. 附带发现（非本轮范围，建议登记）

1. **本地 `refs/remotes/origin/main` 损坏**：`git fetch` 报成功、`update-ref` 返回 0，但引用仍不可读（`status -sb` 显示 `[gone]`）。**推送本身没问题**（`ls-remote` 显示远端 main = 本地 HEAD）。⇒ 需按已有 skill `git-loose-ref-recover` 单独修；**推送验证一律以 `ls-remote` 为准**。
2. **`strategies.detect()` 统一入口与 7 个研究脚本口径分叉**：它们仍按单参调用（`symbol=None` ⇒ 旧 9.8% 口径）。⇒ 研究侧结论与生产侧不一致，需在 P1 统一。
3. **生产工作区有大量未提交改动**（`git status` 数十个 `M`/`??`）⇒ HEAD ≠ 工作区，任何「从 HEAD 复现生产」的尝试都会不同。

---

## 9. 下一步（自然衔接）

1. **9/15 09:15 前的最后确认**：`load_pattern_pool('2026-09-15')` 非 None + 池 `built_at` 稳定（自动化 `b35c444e` 09:40 会核对竞价留痕；池本身需人扫一眼）。
2. **9/15 收盘后**：用当日**真实的 `confirm_queue`** 做第一次「队列 vs 选手当日候选池」对照 —— 这才是新选股层的第一个真样本日。
3. `player_fills` 与 `lookback` 两项待裁定。
