# EvoAlpha 根仓库 .gitignore（1A-RO 第零步）

# 已有独立 git repo（避免嵌套）
yaoban-system/

# 大体量只读资料源（由 learning/manifests source manifest SHA-256 管辖）
妖板选手方法论拆解/
选手学习资料/**/*.mp4

# 大目录：运行时/第三方/研究 venv
Vibe-Research/
py_libs/
因子研究/
data/

# 学习资料中的视频不入库（PNG/MD 保留）
*.mp4

# 生产交易与进化目录（Phase 1C/1D 前不施工；trading/evolution/risk 近空）
# 注：agents/ 与 outputs/ 属 EvoAlpha 控制平面历史产物，入库

# Python
__pycache__/
*.pyc
.venv/
venv/

# 系统文件
.DS_Store
Thumbs.db

# WorkBuddy 会话数据
.workbuddy/

# ---- 2026-09-15 追加（用户裁定）----

# 看板运行时数据：status.json 每次刷新都变 ⇒ 入库只产生 git 噪音，且它是"数据"不是"文档"。
# 定位：docs/dashboard/status.json = 看板唯一真相源（被 tools/build_roadmap_dashboard.py、
#       tools/roadmap_server.py 读取）。已移出版本控制；备份见 evoalpha_all/_dashboard_backup/。
docs/dashboard/

# 临时工作区：一次性脚本、证据快照、修复前备份。
# 约定：此处内容**随时可删**，需要长期保留的产物必须移出本目录并登记到 docs/_INDEX.md。
_scratch/

# learning/tools 下的一次性脚本与草稿（下划线前缀约定 = 临时，可随时删）
learning/tools/_*

# 画像产物：由 yaoban-system/tools/build_persona_*.py 生成（OUT_MD 写死 docs/PERSONA_*.md）
# ⇒ 入库只会造成"生成器改一次、产物再改一次"的双写噪音。要改内容请改生成器。
docs/PERSONA_*.md
